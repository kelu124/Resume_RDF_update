# Core library (always required)
anthropic>=0.28.0

# Streamlit web application  (pip install "resume-rdf[app]")
streamlit>=1.35.0

# Turtle RDF validation       (pip install "resume-rdf[validate]")
rdflib>=6.0.0

# Dataset download            (pip install "resume-rdf[dataset]")
datasets>=2.0.0
huggingface_hub>=0.20.0
